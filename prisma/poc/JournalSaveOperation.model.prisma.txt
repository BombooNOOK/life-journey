/**
 * Prisma model draft for JournalSaveOperation (4B-4P).
 * Mirrored into prisma/schema.prisma for client generate.
 * Formal prisma/migrations/ promotion is GATED on Preview DB isolation (currently unknown → B).
 */

/*
model JournalSaveOperation {
  id                 String    @id @default(cuid())
  /// Normalized viewer-email today (Journal ownership). Not named "email" to allow future identity migration.
  actorKey           String
  saveOperationId    String
  status             String
  checkpoint         String
  journalEntryId     String?
  requestFingerprint String
  resultCode         String?
  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  completedAt        DateTime?

  @@unique([actorKey, saveOperationId])
  @@index([actorKey, createdAt])
}
*/
